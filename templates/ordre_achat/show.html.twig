{% extends 'base.html.twig' %}

{% block title %}OrdreAchat{% endblock %}

{% block body %}
    <h1>OrdreAchat</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ordre_achat.id }}</td>
            </tr>
            <tr>
                <th>Automatique</th>
                <td>{{ ordre_achat.Automatique ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>MontantMax</th>
                <td>{{ ordre_achat.MontantMax }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ ordre_achat.Date ? ordre_achat.Date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ordre_achat_index') }}">back to list</a>

    <a href="{{ path('ordre_achat_edit', {'id': ordre_achat.id}) }}">edit</a>

    {{ include('ordre_achat/_delete_form.html.twig') }}
{% endblock %}
